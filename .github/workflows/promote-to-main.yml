name: Promover qualidade → main (Produção)

on:
  workflow_dispatch:

jobs:
  promote:
    name: Merge qualidade into main
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Merge qualidade into main
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"

          git fetch origin qualidade main

          git checkout main
          git merge --no-ff origin/qualidade -m "chore: promote qualidade to main (production)"
          if git push origin main; then
            echo "Promoted qualidade to main (production) successfully."
            exit 0
          fi

          echo "Direct push failed. Branch protection may be active."
          exit 1
