name: Seed Cardápio

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/seed-cardapio.yml'
    branches: [main]

jobs:
  seed:
    name: Inserir produtos do cardápio
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL || 'postgresql://postgres.dakleqewbwbryuchlrzm:LKB2DWbWbc60fZXh@aws-1-eu-west-1.pooler.supabase.com:6543/postgres' }}
      PGSSLMODE: require

    steps:
      - name: Install psql
        run: sudo apt-get install -y postgresql-client

      - name: Inserir produtos do cardápio (trigger)
        run: |
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c "
            INSERT INTO produtos (nome, preco, categoria, ordem, ativo) VALUES
              ('Hambúrguer',      2500, 'menu',  1, true),
              ('Combo',          4000, 'menu',  2, true),
              ('Mufete Pequeno', 3500, 'menu',  3, true),
              ('Mufete Grande',  5000, 'menu',  4, true),
              ('Paito Alto Pequeno', 3500, 'menu', 5, true),
              ('Paito Alto Grande',  5000, 'menu', 6, true),
              ('Calulu Pequeno', 3500, 'menu',  7, true),
              ('Calulu Grande',  5000, 'menu',  8, true),
              ('Composto Bife Pequeno',          3500, 'menu',  9, true),
              ('Composto Bife Grande',           5000, 'menu', 10, true),
              ('Composto Febra Pequeno',         3500, 'menu', 11, true),
              ('Composto Febra Grande',          5000, 'menu', 12, true),
              ('Composto Peito de Frango Pequeno', 3500, 'menu', 13, true),
              ('Composto Peito de Frango Grande',  5000, 'menu', 14, true),
              ('Composto Frango Pequeno',        3500, 'menu', 15, true),
              ('Composto Frango Grande',         5000, 'menu', 16, true)
            ON CONFLICT DO NOTHING;

            UPDATE produtos SET categoria='menu'
            WHERE nome IN (
              'Hambúrguer','Combo',
              'Mufete Pequeno','Mufete Grande',
              'Paito Alto Pequeno','Paito Alto Grande',
              'Calulu Pequeno','Calulu Grande',
              'Composto Bife Pequeno','Composto Bife Grande',
              'Composto Febra Pequeno','Composto Febra Grande',
              'Composto Peito de Frango Pequeno','Composto Peito de Frango Grande',
              'Composto Frango Pequeno','Composto Frango Grande'
            );
          "
          echo "Produtos inseridos e categoria actualizada. [run-2]"
