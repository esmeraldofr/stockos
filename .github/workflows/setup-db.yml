name: Setup Database

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  setup:
    name: Initialize Supabase Database
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL || 'postgresql://postgres:LKB2DWbWbc60fZXh@db.dakleqewbwbryuchlrzm.supabase.co:5432/postgres' }}
      PGSSLMODE: require

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install psql
        run: sudo apt-get install -y postgresql-client

      - name: Check if all tables exist
        id: check
        run: |
          COUNT=$(psql "$DATABASE_URL" -t -c \
            "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('utilizadores','produtos','turnos','turno_stock','turno_caixa');" \
            2>/dev/null | tr -d ' \n' || echo "0")
          echo "count=$COUNT" >> $GITHUB_OUTPUT
          echo "Tables found: $COUNT"

      - name: Run SQL setup
        if: steps.check.outputs.count != '5'
        run: |
          echo "Missing tables (found ${{ steps.check.outputs.count }}/5). Running SQL..."
          psql "$DATABASE_URL" -f supabase/stockos_database.sql
          echo "Database setup complete!"

      - name: Already setup
        if: steps.check.outputs.count == '5'
        run: echo "All 5 tables exist. Skipping."

      - name: Set admin password
        run: |
          psql "$DATABASE_URL" -c "
            UPDATE utilizadores SET senha_hash = encode(
              digest('admin123' || 'stockos-pwd-salt-2025', 'sha256'), 'hex'
            ) WHERE email = 'admin@stockos.ao';
          "
          echo "Admin password set."
